import { RequestInterceptorResult } from '../../types';
/**
 * @todo 添加前缀和后缀拦截器
 *
 * @param {*} url
 * @param {*} options
 * @return {*}  {RequestInterceptorResult}
 */
export declare function addRequestInterceptor(url: any, options: any): RequestInterceptorResult;
